{
  "compilerOptions": {
    "target": "ES6",                // Define a versão do JavaScript de saída
    "module": "commonjs",           // Define o tipo de módulo (commonjs é o padrão para Node.js)
    "outDir": "./dist",             // Define o diretório de saída para os arquivos JS
    "rootDir": "./src",             // Diretório de origem dos arquivos TypeScript
    "esModuleInterop": true,        // Facilita a importação de módulos CommonJS no estilo ES6
    "strict": true,                 // Ativa todas as checagens estritas de tipo
    "skipLibCheck": true,           // Ignora checagens em arquivos de definição de tipos
    "forceConsistentCasingInFileNames": true // Garante consistência no uso de maiúsculas/minúsculas
  },
  "include": [
    "src/**/*.ts"                   // Inclui todos os arquivos .ts dentro da pasta src
  ],
  "exclude": [
    "node_modules"                  // Exclui a pasta node_modules
  ]
}
